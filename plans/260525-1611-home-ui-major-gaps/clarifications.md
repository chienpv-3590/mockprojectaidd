# Clarifications

## Session 2026-05-25
- Q: Scope for polish plan? → A: Major gaps only — #1 (B4 section) + #2 (award card text overlay). Other 6 gaps (countdown size, hero typography, event info styling, FAB dimensions, header nav visibility, per-section spacing) deferred to future polish pass.
- Q: B4 Vietnamese description copy — exact text? → A: Cannot OCR from design preview cleanly. Use spec hint + plausible brand-spirit copy. Operator to swap for real copy when available.
- Q: Award card title `h3` removal vs keep? → A: Remove or shrink — text is now baked into the overlay image. Description + "Chi tiết" link stay below.
- Q: B4 placement in page? → A: Between `<Hero>` and `<AwardsGrid>` in `app/page.tsx`.
